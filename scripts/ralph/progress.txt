# Ralph Progress Tracker — Aster P0 Fullstack
# Branch: ralph/aster-p0-fullstack
# Started: 2026-05-21
# Total stories: 58
# Completed: 11
# Last updated: 2026-05-21

US-001: DONE — Added TimeState struct to engine-core with delta_seconds, fixed_delta_seconds, total_time, frame_index, time_scale fields; update(dt) method; and 4 unit tests
US-002: DONE — InputState already existed in engine-platform with HashSet-based key tracking, pressed/held/released semantics, mouse/cursor/scroll tracking, end_frame() reset, and query methods; 2 tests pass
US-003: DONE — Added ActionMap struct with HashMap<String, Vec<KeyCode>> bindings, bind() method, action_pressed()/action_held() queries, default WASD/arrows/Space bindings, and 4 unit tests
US-004: DONE — Added ProjectContext struct with manifest, scene, asset_db, project_root fields; open() loads aster.project.toml, parses default scene JSON, and scans asset root; test validates example project opens successfully
US-005: DONE — Added instance and adapter fields to WgpuRenderDevice; added new(window) constructor; HighPerformance adapter with fallback; Bgra8UnormSrgb swapchain format preference
US-006: DONE — Surface and swapchain already existed on WgpuRenderDevice (surface, surface_config fields, create_surface from winit Window, Fifo present mode, Bgra8UnormSrgb format, resize_surface/ensure_surface_depth methods). Added integration test verifying surface creation from real winit window.
US-007: DONE — Added surface_suspended flag to WgpuRenderDevice; resize_surface now suspends rendering on zero-area (w=0 or h=0) and resumes when valid dimensions arrive; surface rendering path checks suspended flag; depth resources dropped before surface reconfiguration.
US-008: DONE — Extended Vertex with normal and uv; added DebugMesh enum (Cube, Sphere(u32), Plane); added MeshBuffers struct; added generate_mesh/generate_sphere/generate_plane helpers; added create_mesh_buffers() to WgpuRenderDevice; updated cube to 24 hard-normal vertices + 36 u32 indices; 6 unit tests pass
US-009: DONE — Replaced placeholder shader with proper PBR-style WGSL shader: vs_main transforms by MVP matrix and passes normal/uv to fragment; fs_main computes diffuse+ambient lighting with optional texture sampling; bind group layout: camera uniform (b0), model uniform (b1), material uniform (b2), texture (b3), sampler (b4); default 1x1 white texture and sampler created; vertex layout now includes normal (loc 1) and uv (loc 2); added CameraUniform/ModelUniform/MaterialUniform structs and matrix helpers
US-010: DONE — Added RenderWorld::extract(&Scene) method that iterates active GameObjects, extracts Camera→RenderCamera (fov/near/far), MeshRenderer→RenderObject (mesh/material names with builtin-or-asset resolution), and Light→RenderLight (kind/intensity); inactive and non-renderable objects skipped; unit test verifies 1 camera + 2 mesh objects extracted correctly
US-011: DONE — Render pipeline execution was already implemented in submit_render_world (acquire swapchain, clear pass, opaque pass with instanced draw, camera/model uniform updates, present). Added integration test that creates WgpuRenderDevice from window, submits RenderWorld with camera + debug cube, and verifies no error + submitted_worlds==1; 8 tests pass
US-012: DONE — Added RenderService struct to engine-editor with Box<dyn RenderDevice> and HashMap<String, RenderTargetDesc>; new() creates default scene_view/game_view targets; render_target() queries by name; resize_target() updates dimensions; render_to_target() creates target, submits world, destroys target; 5 tests pass
US-013: DONE — Integrated wgpu render texture into egui Scene View: added Arc<Device>/Arc<Queue> sharing to WgpuRenderDevice with from_arc_device constructor and device_arc/queue_arc accessors; added render_world_offscreen() for surface-bypassing offscreen rendering; added ViewportTexture to ShellUiState; integrated WgpuRenderDevice into CLI's App; render_scene_view() renders 3D scene to offscreen texture each frame and registers via update_egui_texture_from_wgpu_texture; draw_render_viewport displays rendered texture as egui Image; added editor camera orbit/pan/zoom controls (right-click drag rotate, middle-click pan, scroll zoom); quat_look_at helper for look-at rotation; all tests pass, workspace compiles
