model Crate {
  mesh = primitive.box(size: [1, 1, 1])

  material = material {
    base_color = "#8a5a2b"
    roughness = 0.75
    metallic = 0.0
  }

  collider = collider.box {
    size = [1, 1, 1]
  }

  rigidbody = dynamic {
    mass = 12kg
  }
}
