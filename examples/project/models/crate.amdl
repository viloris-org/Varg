amdl

model Crate {
  mesh {
    kind = primitive.box
    size = [1, 1, 1]
  }

  material {
    kind = inline
    base_color = "#8a5a2b"
    roughness = 0.75
    metallic = 0.0
  }

  collider {
    shape = box
    size = [1, 1, 1]
  }

  rigidbody {
    mode = dynamic
    mass = 12kg
  }
}
