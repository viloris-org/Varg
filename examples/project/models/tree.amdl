amdl

model Tree {
  mesh {
    kind = asset
    path = "models/tree.glb"
  }

  material {
    kind = ref
    path = "materials/bark.amat"
  }

  collider {
    shape = cylinder
    radius = 0.4m
    height = 3.0m
  }

  rigidbody {
    mode = static
  }
}
